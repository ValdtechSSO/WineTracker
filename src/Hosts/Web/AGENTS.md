# Web host

Deliver the WineTracker browser experience with Angular and Angular Material.
All interactive components use Angular Material. Product decisions remain in
WineJournal and are accessed through the API.

TypeScript architecture is enforced by this project's Angular build, lint, and
tests until a separately versioned AAK Angular adapter is justified and pinned.
